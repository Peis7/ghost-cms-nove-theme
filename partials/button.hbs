<button class="bg-black py-6 px-12 text-white rounded-full {{class}}">
    <a href="{{url}}" class="block">
        <span class="text-white float-left text-base">{{title}}</span>
        {{#if icon_src}}
            <img class="pl-4" src={{icon_src}} />
        {{/if}}
    </a>
</button>